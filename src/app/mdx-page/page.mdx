import { MyComponent } from '@/components/my-component'

# Welcome to my MDX page!

This is an example of a single MDX page in a Next.js application

We can even import and use React components or other `.mdx` pages here:

<MyComponent />
